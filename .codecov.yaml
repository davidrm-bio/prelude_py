# Based on pydata/xarray
codecov:
  require_ci_to_pass: no

coverage:
  status:
    project:
      default:
        # Require 50% coverage
        target: 50
    changes: false

comment:
  layout: diff, flags, files
  behavior: once
  require_base: false
